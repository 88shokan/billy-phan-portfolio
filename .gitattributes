# Normalise line endings. Without this, the same file can look entirely
# rewritten in a diff just because it was edited on a different OS.
* text=auto eol=lf

# Binary assets git should never try to normalise
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.ico binary
