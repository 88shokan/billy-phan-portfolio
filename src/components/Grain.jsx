/* Paper tooth over the whole page. Purely decorative; the texture and
   its 15fps jitter are entirely in the stylesheet. */
export default function Grain() {
    return <div className="grain" aria-hidden="true" />;
}
